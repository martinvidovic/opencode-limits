{
  "name": "opencode-limits",
  "version": "0.1.0",
  "description": "Native OpenCode TUI popup for connected provider usage limits",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    "./server": {
      "types": "./dist/server.d.ts",
      "import": "./dist/server.js"
    },
    "./tui": {
      "types": "./dist/tui.d.ts",
      "import": "./dist/tui.js"
    }
  },
  "engines": {
    "node": ">=22.14.0",
    "opencode": ">=1.14.42 <2"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:package": "node scripts/verify-package.mjs",
    "check": "npm run typecheck && npm run lint && npm run format:check && npm run test && npm run build && npm run verify:package",
    "prepublishOnly": "npm run check"
  },
  "devDependencies": {
    "@linters/eslint-config-typescript": "2.5.1",
    "@linters/prettier-config": "2.1.1",
    "@opencode-ai/plugin": "1.15.12",
    "@types/node": "22.13.4",
    "eslint": "9.39.2",
    "eslint-config-prettier": "10.1.8",
    "prettier": "3.5.3",
    "typescript": "5.8.2",
    "vitest": "3.0.8"
  }
}
